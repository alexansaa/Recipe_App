<div>
  <%= render @food %>
  <div>
    <%= link_to "Back to foods", foods_path %>
    <% if can? :destroy, @food %>
      <%= button_to "Destroy this food", @food, method: :delete %>
    <% end %>
  </div>
</div>
